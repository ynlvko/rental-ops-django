[project]
name = "rental-ops-django"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Yurii Nalyvaiko", email = "yura.nlvko@gmail.com" }]
requires-python = ">=3.14"
dependencies = ["django>=6.1"]

[project.scripts]
rental-ops-django = "rental_ops_django:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
